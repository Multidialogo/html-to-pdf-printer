services:
  app:
    build:
      target: builder
    ports:
      - "5000:5000"
    volumes:
      - ./src:/src
      - ./nginx.conf:/etc/nginx/nginx.conf:ro
    entrypoint: [ "/src/entrypoint.dev.sh" ]